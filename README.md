## CNN Autoencoder for Anomaly Detection

This project implements a CNN-based autoencoder for unsupervised anomaly detection.

### Phase 1: Fashion-MNIST Baseline
- Autoencoder trained on a single normal class
- Anomaly score: reconstruction error (MSE)
- Evaluation using ROC-AUC

This baseline validates the methodology before extending it to scientific datasets.